# 📁 프로젝트 파일 구조 안내

> Next.js 프레임워크 규칙상 **코드 폴더(`app/`, `lib/` 등)는 한글로 바꿀 수 없습니다.**
> 대신 이 파일에서 각 폴더가 어떤 역할을 하는지 한글로 설명합니다.

---

## 🗂️ 폴더 구조 한눈에 보기

```
Backlink-shop/
├── 📄 문서/                    ← 모든 .md 문서 (기획/배포/개발 등)
├── 💻 app/                    ← 웹페이지 & API 라우트 (화면에 보이는 모든 것)
├── 🧰 lib/                    ← 재사용 유틸리티/비즈니스 로직
├── 🖥️ server/                 ← 서버 전용 코드 (Supabase admin 등)
├── 🎨 public/                 ← 이미지·정적 파일 (브라우저가 직접 접근)
├── 📧 emails/                 ← 이메일 템플릿 (Resend용)
├── 🔧 scripts/                ← 일회성/유지보수 스크립트
├── 🗃️ supabase/               ← DB 마이그레이션 & 타입 정의
├── 🧪 tests/                  ← 테스트 코드
├── 📐 types/                  ← 전역 TypeScript 타입
├── ⚙️ (설정 파일들)            ← package.json, tsconfig 등
└── 🔒 .husky/ .next/ 등        ← 도구 자동 생성 (건드리지 않음)
```

---

## 💻 `app/` — 웹페이지와 API (Next.js App Router)

사용자가 보는 화면과 호출하는 API가 전부 여기 있습니다.

| 경로                    | 역할                                         |
| ----------------------- | -------------------------------------------- |
| `app/page.tsx`          | 메인 홈페이지                                |
| `app/lp/seo/`           | **랜딩페이지** — `/lp/seo` 경로. SEO 분석 폼 |
| `app/analyze/[domain]/` | **분석 결과 페이지** — 폼 제출 후 결과 표시  |
| `app/api/lp/request/`   | 분석 요청 API (폼 submit 받는 곳)            |
| `app/api/lp/status/`    | 분석 상태 폴링 API                           |
| `app/api/cache/`        | 도메인/SERP/백링크 캐시 API                  |

> Next.js 규칙: 폴더 이름 = URL 경로. `app/lp/seo/page.tsx` → `/lp/seo`

---

## 🧰 `lib/` — 재사용 가능한 로직

| 파일                         | 역할                                               |
| ---------------------------- | -------------------------------------------------- |
| `lib/competitor-analyzer.ts` | 경쟁사 분석 핵심 로직 (Serper + RapidAPI + VebAPI) |
| `lib/seo-analyzer.ts`        | HTML 파싱 & 온페이지 SEO 분석                      |
| `lib/lp-metrics.ts`          | LP 결과 페이지용 메트릭 계산 (KPI/격차)            |
| `lib/domain.ts`              | 도메인 정규화 유틸                                 |
| `lib/api/goat-pbn.ts`        | Goat PBN 연동                                      |
| `lib/supabase/`              | Supabase 클라이언트                                |
| `lib/email/`                 | 이메일 전송 로직                                   |

---

## 🖥️ `server/` — 서버 전용 코드

브라우저에 노출되면 안 되는 서버 전용 로직.

- `server/supabase/admin.ts` — Service Role 키를 쓰는 Supabase admin 클라이언트

---

## 🗃️ `supabase/` — 데이터베이스

- `supabase/migrations/` — DB 마이그레이션 SQL
- `supabase/` 하위 타입 정의 — TypeScript 테이블 타입

---

## 📄 `문서/` — 모든 문서 정리

| 폴더                   | 내용                                                      |
| ---------------------- | --------------------------------------------------------- |
| `문서/01-기획/`        | PRD, 캠페인, 랜딩 가이드, 구글 ads 전략 등                |
| `문서/02-배포/`        | Vercel 배포, 배포 체크리스트, 환경변수                    |
| `문서/03-개발/`        | 이메일 설정, PBN 연동, 성능 최적화, 버그픽스 기록         |
| `문서/04-API참고자료/` | VebAPI / RapidAPI / Serper 엔드포인트 문서 (구 `PROMPT/`) |
| `문서/05-대화기록/`    | Claude와의 중요 논의 요약                                 |
| `문서/99-구버전/`      | 더 이상 안 쓰는 구버전 문서 (확인 후 삭제 가능)           |

---

## ⚙️ 루트의 주요 설정 파일

| 파일                      | 역할                                               |
| ------------------------- | -------------------------------------------------- |
| `package.json`            | 프로젝트 의존성, npm 스크립트                      |
| `next.config.js` / `.mjs` | Next.js 설정                                       |
| `tailwind.config.ts`      | Tailwind CSS 설정                                  |
| `tsconfig.json`           | TypeScript 설정                                    |
| `middleware.ts`           | 모든 요청을 가로채는 미들웨어 (인증/리다이렉트 등) |
| `.env.local`              | 로컬 환경변수 (비밀키 — git 제외)                  |
| `.env.example`            | 환경변수 샘플 (팀 공유용)                          |

---

## 🔒 건드리지 말 것

- `.next/` — Next.js 빌드 산출물 (자동 생성)
- `node_modules/` — npm 패키지 (자동 생성)
- `.husky/` — git hook
- `.git/` — git 내부
- `tsconfig.tsbuildinfo` — TypeScript 증분 빌드 캐시

---

## 🚀 자주 쓰는 명령어

```bash
npm run dev          # 개발 서버 시작 (localhost:3000)
npm run build        # 배포용 빌드
npm run lint         # 코드 검사
npm run test         # 테스트 실행
```
